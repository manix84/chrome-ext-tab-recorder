# 🖼️ Store Assets

Chrome Web Store listing screenshots are generated at `1280x800`.

Regenerate them with:

```sh
npm run screenshots
```

The source layout lives in `store-assets/screenshot-template.html`, and generated PNGs are written to `store-assets/screenshots/`.
